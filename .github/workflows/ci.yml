# ═══════════════════════════════════════════════════════════════════════════════
# CI — Integración Continua
# Deltos — Sistema de Gestión de Proyectos Académicos
# ═══════════════════════════════════════════════════════════════════════════════
#
# Disparadores:
#   • Cualquier push a `master`
#   • Cualquier Pull Request que apunte a `master`
#
# Jobs:
#   1. backend-ci  → install → type-check → test (unitarios + integración) → build
#   2. frontend-ci → install → build (Vite)
#
# Los jobs corren en paralelo para ahorrar tiempo.
# ═══════════════════════════════════════════════════════════════════════════════

name: CI — Integración Continua

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

# Cancelar ejecuciones anteriores del mismo PR/branch para no desperdiciar minutos
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ─────────────────────────────────────────────────────────────────────────────
  # JOB 1: Backend — TypeScript build + tests completos
  # ─────────────────────────────────────────────────────────────────────────────
  backend-ci:
    name:  Backend CI (Node.js ${{ matrix.node-version }})
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20]   # Añadir más versiones aquí para matrix testing

    defaults:
      run:
        working-directory: backend

    steps:
      # 1. Clonar el repositorio
      - name:  Checkout del código
        uses: actions/checkout@v4

      # 2. Configurar Node.js con caché de npm
      - name: ️ Configurar Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm
          cache-dependency-path: backend/package-lock.json

      # 3. Instalar dependencias exactas del lockfile
      - name:  Instalar dependencias (npm ci)
        run: npm ci

      # 4. Verificar tipos TypeScript sin emitir archivos
      #    Detecta errores de tipos antes de compilar
      - name:  Verificación de tipos TypeScript
        run: npx tsc --noEmit

      # 5. Tests unitarios (sin BD real — todo mockeado)
      - name:  Tests unitarios (ProyectoService, EventoService, AuthService)
        run: npm test -- --testPathPattern="unit" --no-coverage --forceExit
        env:
          NODE_ENV: test
          JWT_SECRET: ${{ secrets.JWT_SECRET || 'ci-test-secret-super-seguro-12345' }}
          JWT_EXPIRES_IN: 15m
          JWT_REFRESH_SECRET: ${{ secrets.JWT_REFRESH_SECRET || 'ci-refresh-secret-super-seguro-12345' }}
          JWT_REFRESH_EXPIRES_IN: 7d
          DATABASE_URL: mysql://root:ci_password@127.0.0.1:3306/gestor_test

      # 6. Tests de integración (supertest + mock de Prisma — sin BD real)
      - name:  Tests de integración (Auth endpoints HTTP)
        run: npm test -- --testPathPattern="auth.test" --no-coverage --forceExit
        env:
          NODE_ENV: test
          JWT_SECRET: ${{ secrets.JWT_SECRET || 'ci-test-secret-super-seguro-12345' }}
          JWT_EXPIRES_IN: 15m
          JWT_REFRESH_SECRET: ${{ secrets.JWT_REFRESH_SECRET || 'ci-refresh-secret-super-seguro-12345' }}
          JWT_REFRESH_EXPIRES_IN: 7d
          DATABASE_URL: mysql://root:ci_password@127.0.0.1:3306/gestor_test

      # 7. Reporte de cobertura de código
      - name:  Reporte de cobertura (jest --coverage)
        run: npm run test:coverage -- --forceExit
        env:
          NODE_ENV: test
          JWT_SECRET: ${{ secrets.JWT_SECRET || 'ci-test-secret-super-seguro-12345' }}
          JWT_EXPIRES_IN: 15m
          JWT_REFRESH_SECRET: ${{ secrets.JWT_REFRESH_SECRET || 'ci-refresh-secret-super-seguro-12345' }}
          JWT_REFRESH_EXPIRES_IN: 7d
          DATABASE_URL: mysql://root:ci_password@127.0.0.1:3306/gestor_test

      # 8. Subir el reporte de cobertura como artefacto descargable
      - name:  Subir reporte de cobertura
        uses: actions/upload-artifact@v4
        if: always()   # Subir incluso si los tests fallaron
        with:
          name: coverage-report
          path: backend/coverage/
          retention-days: 7

      # 9. Compilar TypeScript → JavaScript (verifica que el build funcione)
      - name: ️ Build de producción (tsc)
        run: npm run build

      # 10. Subir el dist como artefacto (útil para el job de CD)
      - name:  Subir artefacto de build
        uses: actions/upload-artifact@v4
        with:
          name: backend-dist
          path: backend/dist/
          retention-days: 1

  # ─────────────────────────────────────────────────────────────────────────────
  # JOB 2: Frontend — instalar dependencias y build de Vite
  # ─────────────────────────────────────────────────────────────────────────────
  frontend-ci:
    name:  Frontend CI (Vite build)
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: frontend

    steps:
      - name:  Checkout del código
        uses: actions/checkout@v4

      - name: ️ Configurar Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name:  Instalar dependencias (npm ci)
        run: npm ci

      # Verificar que el build de producción de Vite termina sin errores
      - name: ️ Build de producción (Vite)
        run: npm run build

      - name:  Subir artefacto del dist
        uses: actions/upload-artifact@v4
        with:
          name: frontend-dist
          path: frontend/dist/
          retention-days: 1
